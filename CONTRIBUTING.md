# Contributing

## Setup

```bash
npm install
```

## Checks

```bash
npm test
npm run typecheck
npm run build
```

## Notes

- Tests use Vitest (`vitest run`).
- Prefer small, focused changes with regression coverage for agent loop behavior (`maxTurns`, abort, tool timeouts).
